{
  "name": "vault client",
  "description": "demo reading from a vault server",
  "repository": "https://github.com/pallavkothari/vault-client",
  "keywords": ["vault", "client"],
  "env": {
    "VAULT_TOKEN": {
      "description": "vault token",
      "required": true
    },
    "VAULT_HOST": {
      "description": "vault host",
      "required": true
    }
}
